// The former fixed candidate-writer wizard no longer exists. Keep this legacy
// test entrypoint as an alias for the native Agent choice-flow UI acceptance so
// release scripts and local shortcuts continue to work.
await import("./test-conversation-agent-ui.mjs");
