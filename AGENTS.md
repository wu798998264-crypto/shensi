# Workspace Rules

- 默认只搜索 `src`、`server.mjs`、`scripts`、`packaging/windows`，以及与当前任务直接相关的测试。
- 禁止无过滤地递归扫描 `node_modules`、`runtime`、`artifacts`、`output`、`dist`、`release`、`test-results`。
- 禁止整文件读取 `src/app.js`、`server.mjs`、`src/server/shensi-orchestrator.mjs`。
- 先使用 `rg -n` 定位符号；每次读取不超过 250 行，需要继续时再读取下一段。
- 普通消息中出现“skill”不等于调用 Skill；只有用户明确调用具体 Skill，或任务语义确实需要时才加载。
- 默认只使用一个主 Skill；不要重复加载相同 Skill。
- 未经用户明确要求，不使用子 Agent 或多 Agent。
- 不重复已经完成的搜索和检查。
- 日常修改只运行受影响的定向测试；完整 `npm test` 只用于发布或用户明确要求。
- 工具输出必须限行，只保留错误摘要和相关代码位置。
- 回答、诊断、计划任务不得擅自修改业务代码。

## 受保护能力：跨配置串行生图

- “聚合 API → 除短剧最前线外的即梦配置逐号生图 → 切回柏物语”的严格串行流程是受保护能力；每张图片必须在下载、尺寸和 SHA-256 验收完成后才能切换下一配置。
- 任何可能改变即梦凭证锁、账号隔离、配置切换、图片提交/轮询/下载、幂等恢复或聚合 API 生图路由的修改，编辑前都必须单独向用户请示，说明影响范围、回退风险和必跑回归；不得从普通功能修改授权中推定许可。
- 获准修改后至少运行 `npm run test:protected-image-profile-switch`；真实跨账号付费回归只能在用户明确授权消耗积分/API 后运行，并且必须排除“短剧最前线”。
- 不得为测试修改、覆盖或迁移现有供应商配置、端点、模型和凭证；失败步骤不得跳过或盲目重提厂商任务。
