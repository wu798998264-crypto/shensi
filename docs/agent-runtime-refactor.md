# Agent 运行模式改造阶段记录

## 本地版本管理

- 基线提交：`40b0fb0`，当前源码（含前一轮已验证的历史和限免配置改动）。
- 标签：`baseline-before-agent-runtime-refactor`。
- 分支：`refactor/agent-runtime`。
- 无远端、不推送；不强制重置。签名证书、实际配置、用户作品、运行状态、依赖、日志、安装包均不纳入 Git。
- 生产能力包的规则源码追加纳入版本管理，原始资料、测试工程和废弃资料排除。

## 最新保留约束

- 白板配置与现有生成能力不变。
- 多对话可同时运行；同一对话保留有序队列，不引入全局串行锁。
- 保留多候选：通过自然语言描述数量、差异，不再询问主笔身份或数量。
- 保留四种写入规则、完整历史版本、经验与记忆检查、作品隔离。
- 对话媒体默认参数与入口改造已获独立授权；不得做未授权的真实付费回归。
- 开发者作品、测试内容不得打包，既检查文件路径，也检查生产初始状态。

## 阶段

1. `da4ec97`：发布路径允许清单、移除生产测试种子、关闭不安全复制回退；package-content 与 check.mjs 定向检查通过。
2. 发布补强：旧样本文本改哈希、不再生成假演示回复；发布目录重建能力清单后验证。独立临时目录验证 201 个运行文件，不含 397 个原始/参考隔离文件。
3. 待完成：自然语言多候选与统一选择框事件时序。
4. 待完成：Agent 自主工具循环、上下文/Skill 按需读取、外置接管、内置内核接入。
5. 待完成：对话媒体默认值、资产自动备份回归、多对话并发和重启恢复回归。

## 回退

先保存新改动，再使用 `git revert <阶段提交>` 生成反向提交。依赖阶段按提交倒序回退；不得使用 `reset --hard` 或删除工作区。也可以在新的本地 worktree 中检出基线标签作对照，但不可令旧版服务并发写同一作品目录。Git 回退源码不回退用户作品或厂商任务。
