export const BUNDLED_CUSTOM_SKILL_SEED_VERSION = 4;

export const STYLE_DISTILLATION_SKILL = Object.freeze(
  {
    id: "shensi.style-distillation",
    version: "1.0.0",
    content: `---
schema_version: 2
id: shensi.style-distillation
name: 文风蒸馏
version: 1.0.0
author: 神思团队
description: 从本轮明确提供的授权文本中提炼可迁移的文风档案，或把既有文风档案作为辅助约束交给神思主笔使用。
capability_boundary: 只分析本轮可读样本并提供文风档案或写作约束；不搜索或下载作品，不接管主笔、任务路由、上下文权限、正史、记忆、版本和落盘。
source: bundled_local
capabilities:
  - style_reference
role: auxiliary
workspace_modes:
  - general
artifact_types:
  - style_profile
  - writing_constraints
input_requirements:
  - explicit_text_sample_or_style_profile
  - user_intent
output_contract: style_profile_or_constraints
stages:
  - planning
  - creative
  - revision
conflict_policy: advisory
fallback: builtin
---
# 文风蒸馏

## 定位

把用户本轮粘贴、上传或通过 @ 明确引用的文本，压缩为可复用、可验证、尽量不携带原作内容的文风档案。文风档案是辅助约束，不是模型训练、作者身份模拟或作品事实来源。

本 Skill 没有自动触发词，也不进入任何自定义插槽。只有用户明确 @文风蒸馏 时才参与本轮任务。

## 模式判断

按用户目标选择一种模式：

1. **蒸馏模式**：从样本文本生成文风档案。
2. **应用模式**：读取用户提供或本轮生成的文风档案，把它转成当前任务的写作约束；正文仍由神思主笔生成。
3. **校验模式**：比较目标文本与文风档案，指出吻合项、偏离项和最小修正建议。

用户没有说清模式时，根据最终交付物判断；无法判断时只询问一个最关键问题。

## 输入闸门

- 只分析本轮实际可读的粘贴文本、附件或 @ 文档；外部检索、网络补全和未读取章节均不属于输入。
- 没有样本或文风档案时，要求用户粘贴、上传或 @ 引用材料，不凭作者名生成所谓精准文风。
- 样本来自多个作者、多个时期、多个题材或质量明显不一致时，先分组；未经用户确认不得混成一个档案。
- 大纲、设定、广告、书评和正文分开识别；默认只把连续正文计入文风样本。
- 记录实际覆盖范围。只读取部分章节时标记“样本文风”，不得声称代表整部作品或作者全部时期。
- 用户要求处理超长材料时，按原顺序连续分块分析，再汇总跨块稳定特征；不得只抽开头和结尾冒充完整覆盖。

## 蒸馏流程

### 1. 清洗与分层

识别章节标题、正文、注释和非正文噪声。保留影响节奏的段落边界、标点和对白结构，但不把具体人物、地点、设定和情节写进风格规则。

### 2. 分块特征提取

每个样本块至少检查：

- 叙事人称、视角稳定度、叙事距离和信息权限；
- 句长分布、长短句组合、标点节奏和句式重复；
- 段落长度、换段触发点、留白和场景切换；
- 对白、动作、心理、环境与解释性叙述的比例关系；
- 词汇层级、口语度、抽象度、时代感和术语密度；
- 感官通道、意象来源、修辞种类及密度；
- 情绪表达的直接度、克制度和人物内外显比例；
- 场景推进速度、信息释放、冲突升级和章末收束；
- 开场、转场、悬念、反转、回扣和结尾的常用机制；
- 高频语言病灶、偶然特征和不应继承的内容性特征。

### 3. 跨块归并

只把多个样本块中重复出现且证据稳定的特征列为核心规则。单块偶发现象进入“低置信候选”，互相冲突的特征按场景条件拆分，不强行平均。

### 4. 去内容化

- 删除专有名词、人物关系、世界观事实、独有情节和标志性原句。
- 证据以位置、统计或转述说明为主，不大段引用原文。
- 生成或改写时主动检查与样本的连续重复表达；不得复用连续 12 个汉字以上的非通用原句。
- 对他人作品优先提炼抽象机制和可迁移参数，不承诺复制特定作者身份。

### 5. 形成可执行档案

规则必须能指导写作，避免只输出“细腻、自然、有画面感”等空泛形容。能量化时给出范围；不能量化时给出触发条件、正例结构和反例结构，但不复制样本原句。

## 文风档案格式

蒸馏模式默认按以下结构输出：

### 文风档案：名称

- 来源状态：自有 / 已授权 / 公版 / 用户未说明
- 样本范围：文件、章节、实际读取字数或可确认范围
- 样本纯度：高 / 中 / 低，并说明污染来源
- 总体置信度：高 / 中 / 低
- 适用范围：题材、视角、场景和篇幅
- 不适用范围：证据不足或不应迁移的场景

#### 核心指纹

列出 5—8 条跨样本稳定且最有辨识度的规则。

#### 可测参数

至少包括句长、段长、对白占比、描写占比、心理占比、修辞密度、信息密度和节奏速度；证据不足时明确标记，不伪造精确数字。

#### 场景规则

分别说明开场、对话、动作、心理、环境、转场、高潮和结尾如何变化。

#### 正向约束

列出主笔应执行的具体动作。

#### 负向约束

列出应避免的表达、密度失控和误模仿风险。

#### 证据映射

说明每条核心规则由哪些样本块支持、稳定度如何。只做短转述，不大段引用。

#### 压缩调用卡

把档案压缩成不超过 600 字的执行卡，供后续与正文主笔共同使用。调用卡必须保留条件化规则、核心参数和负向约束，不携带原作人物、情节和长原句。

## 应用规则

- 应用文风时，先服从用户当前要求、创作合同、已确认设定、连续性和产物格式，再使用文风档案。
- 文风只调整表达层，不擅自改变剧情事实、人物动机、信息权限、章节功能或任务目标。
- 场景与样本差异较大时，保留核心指纹，降低表层词汇模仿，避免机械套用。
- 用户临时调整只约束当前任务，除非用户明确要求长期保存，否则不得写入项目长期规则或记忆。
- 用户要求“只蒸馏”时只输出档案，不附带正文；要求“蒸馏并试写”时先给档案，再给短样张并标记为候选。

## 校验规则

校验模式至少检查：

1. 核心指纹命中率；
2. 可测参数偏差；
3. 场景适配是否自然；
4. 是否出现内容污染或近似复句；
5. 是否损害人物一致性、可读性和项目原有声音；
6. 最小修改能否修复，而不是整段重写。

## 硬边界

- 不负责搜索、下载或获取外部作品。
- 不修改任务路由、资料读取范围或路由版本。
- 不把样本内容写入正史、项目记忆或连续性状态。
- 不执行文件写入、版本保存、智能落盘或项目结构操作。
- 不覆盖神思主笔、效果检查、连续性检查和格式门禁。
- 样本不足时降低结论强度，不用常识补造成“已蒸馏结果”。
`,
  },
);

export const BUNDLED_CUSTOM_SKILLS = Object.freeze([]);
