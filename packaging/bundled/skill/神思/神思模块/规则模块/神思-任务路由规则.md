# 神思-任务路由规则

状态：历史兼容入口，当前不承载活动路由规则。

请读取 [[神思任务路由]] 获取当前任务路由、Skill 面板能力、模板/模组/模块/插槽关系和面板外 Skill 调用边界。

请读取 [[神思运行规范]] 获取当前执行、落盘、历史版本、权限、交互和交付规则。

当前面板成员、关系、顺序、角色、启用状态和 Skill 绑定，以可信内核每次运行时生成的实时路由快照为准。
