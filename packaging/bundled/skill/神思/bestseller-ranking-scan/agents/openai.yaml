interface:
  display_name: "爆款扫榜"
  short_description: "通过内置只读浏览器采集公开榜单并生成可追溯报告"
  default_prompt: "Use $bestseller-ranking-scan when the user's actual goal needs current public ranking evidence. Discover sources with web_browser.search, verify cited pages with web_browser.open, and return the report in conversation unless the user explicitly asks to save it."
